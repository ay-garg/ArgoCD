apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx
  name: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  strategy: {}
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - image: docker.ionginx:trixie-perl
        name: docker-ionginx-87ds9
        ports:
        - containerPort: 80
        resources: {}
status: {}